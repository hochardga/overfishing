# Optional analytics configuration.
# Leave the endpoint and write key blank to disable analytics entirely.
# All VITE_* values ship to the browser, so do not place secrets here.

VITE_ANALYTICS_ENDPOINT=
VITE_ANALYTICS_WRITE_KEY=
VITE_ANALYTICS_PREVIEW=false
